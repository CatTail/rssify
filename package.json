{
  "name": "rssify",
  "version": "0.1.0",
  "dependencies": {
    "debug": "^2.6.0",
    "marked": "^0.3.6",
    "micro": "latest",
    "request": "^2.79.0",
    "request-promise": "^4.1.1",
    "rss": "^1.2.1"
  },
  "scripts": {
    "precommit": "npm run lint",
    "start": "NODE_ENV=production node --harmony-async-await index",
    "dev": "DEBUG=rssify nodemon -x 'node --harmony-async-await' index",
    "lint": "standard",
    "deploy": "now -e RSSIFY_GITHUB_ACCESS_TOKEN=@rssify_github_access_token"
  },
  "devDependencies": {
    "husky": "^0.12.0",
    "standard": "^8.6.0"
  }
}
